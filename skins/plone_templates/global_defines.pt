<!-- 
The expressions that used to generate these globals have been 
moved to (much faster) Python code in CMFPlone/browser/plone.py

!!!! Do not add any more defines here, under penalty of death !!!!
-->

<metal:globaldefine define-macro="defines" 
      tal:define="
        global plone_view context/@@plone;
        global view nocall:view | nocall:plone_view;
        foo plone_view/globalize;
        global template_id options/template_id | template/getId | nothing;
        view_template_id plone_view/getViewTemplateId;
        global isViewTemplate python:template_id==view_template_id;
        global normalizeString nocall:putils/normalizeString;
        " />
