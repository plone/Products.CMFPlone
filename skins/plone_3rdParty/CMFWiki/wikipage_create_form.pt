<html xmlns:tal="http://xml.zope.org/namespaces/tal"
       xmlns:metal="http://xml.zope.org/namespaces/metal"
       metal:use-macro="here/main_template/macros/master">
<body>

<div metal:fill-slot="header"
     tal:define="global noedit python:here.isAllowed('create');
                 global page request/page|nothing;
		 global text request/text|nothing;">
    <div tal:replace="structure here/wikipage_header">
    </div>

</div>

<div metal:fill-slot="main">

    <form method="post"
          action="wikipge_create_handler"
	  enctype="multipart/form-data">
       <input type="hidden" name="page" value="page" />
       <textarea wrap="soft" name="text"
                 rows="#" cols="#"
		 tal:attributes="rows python:test(here.zwiki_height, here.zwiki_height, 18);
		                 cols python:test(here.zwiki_width, here.zwiki_width, 80);
				 readonly python:test(noedit, 'readonly', None);"
                 tal:content="text">wiki content</textarea>
       <br/> Format: <span tal:replace="here/page_type">page type</span>
       <span tal:condition="noedit">Create Disabled</span>
       <input type="submit" name="CreatePage" value="#" size="20"
              tal:attributes="value string: Create $page"
	      tal:condition="not: noedit" />
       <div tal:condition="not: noedit">
          <strong>Log Message</strong> <br />
	  <textarea wrap="soft" name="log" rows="3" cols="80"><textarea>
	  <p>Log for landmark changes - enter a note characterizing your
             change.  It will be connected with the page version,
             exposing the version for browsing in the condensed
	     <a href="wikipage_history">page history</a>
	   </p>
       </div>
    </form>			 
</div>

</body>
</html>
